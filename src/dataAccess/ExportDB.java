package dataAccess;

/**
 * ExportDB will export the Vezorla database data into either a <code>.sql</code> file or <code>.csv</code>
 */
public class ExportDB {
    /**
     * Export all data from the Vezorla database to a <code>.csv</code> file
     */
    public void exportData() {}

    /**
     * Backup all data from the Vezorla database to a <code>.sql</code> file
     */
    public void backup() {}
}
